name: Deploy Branch

on:
  workflow_dispatch:
  workflow_call:
    secrets:
      AWS_PRE_MERGE_ROLE_ARN: { required: true }
      AWS_PRE_MERGE_S3_BUCKET_NAME: { required: true }
      PREMERGE_PARAMETER_PREFIX_STACK_NAME: { required: true }
    outputs:
      stack-name:
        description: The deployed stack name
        value: ${{ jobs.deploy.outputs.stack-name }}
      aws-region:
        description: The region in which the stack was deployed
        value: ${{ jobs.deploy.outputs.aws-region }}

permissions: {}

jobs:
  build:
    name: Build SAM app
    runs-on: ubuntu-latest
    permissions:
      contents: read
    concurrency:
      group: build-development-${{ github.head_ref || github.ref_name }}
    outputs:
      sha_short: ${{ steps.vars.outputs.sha_short }}
      repo_base_dir : ${{ steps.vars.outputs.repo_base_dir }}
    steps:
      - name: Check out repository code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up JDK
        uses: actions/setup-java@be666c2fcd27ec809703dec50e508c2fdc7f6654 # v5.2.0
        with:
          java-version-file: '.sdkmanrc'
          distribution: corretto
          cache: gradle

      - uses: gradle/actions/setup-gradle@0723195856401067f7a2779048b490ace7a47d7c # v5.0.2
        with:
          gradle-version: wrapper
          cache-overwrite-existing: true

      - name: Get output variables
        id: vars
        run: |
          echo "sha_short=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT
          echo "repo_base_dir=$(pwd)" >> $GITHUB_OUTPUT

      - name: Build SAM application
        uses: govuk-one-login/github-actions/sam/build-application@3b0015c58dd0e19e1572fda59dda4184df387d1e
        id: build
        with:
          sam-version: "1.157.1"
          base-dir: ${{ steps.vars.outputs.repo_base_dir }}
          template: infrastructure/lambda/template.yaml
          cache-name: ipv-cri-dl-api-${{ steps.vars.outputs.sha_short }}
          pull-repository: true

  deploy:
    name: Deploy stack
    runs-on: ubuntu-latest
    permissions:
      id-token: write
    timeout-minutes: 15
    needs: build
    concurrency:
      group: deploy-development-${{ github.head_ref || github.ref_name }}
    environment:
      name: development
      url: ${{ steps.deploy.outputs.stack-url }}
    outputs:
      aws-region: ${{ steps.deploy.outputs.aws-region }}
      stack-name: ${{ steps.deploy.outputs.stack-name }}
    steps:
      - name: Deploy stack
        uses: govuk-one-login/github-actions/sam/deploy-stack@3b0015c58dd0e19e1572fda59dda4184df387d1e
        id: deploy
        with:
          sam-deployment-bucket: ${{ secrets.AWS_PRE_MERGE_S3_BUCKET_NAME }}
          aws-role-arn: ${{ secrets.AWS_PRE_MERGE_ROLE_ARN }}
          stack-name: preview-${{ needs.build.outputs.sha_short}}
          cache-name: ipv-cri-dl-api-${{ needs.build.outputs.sha_short}}
          s3-prefix: preview
          pull-repository: true
          delete-failed-stack: true
          tags: |
            cri:component=ipv-cri-dl-api
            cri:stack-type=preview
            cri:application=Lime
            cri:deployment-source=github-actions
          parameters: |
            DeploymentType=pre-merge-integration
            ParameterPrefix=${{ secrets.PREMERGE_PARAMETER_PREFIX_STACK_NAME }}
            Environment=dev
            CommonStackName=driving-permit-common-cri-api-local
